# Kubernetes Docker Registry Secret Template
# 
# SECURITY WARNING: This file is a template only.
# Never commit actual credentials to version control!
# 
# To create the actual secret, run:
# kubectl create secret docker-registry registry-secret \
#   --namespace=crypto-rates-app \
#   --docker-server=192.168.0.100:5000 \
#   --docker-username=${REGISTRY_USERNAME} \
#   --docker-password=${REGISTRY_PASSWORD} \
#   --docker-email=${REGISTRY_EMAIL}
#
# Or use the automation script: ./scripts/01-setup-secure-docker-registry.sh
#
apiVersion: v1
kind: Secret
metadata:
  name: registry-secret
  namespace: crypto-rates-app
type: kubernetes.io/dockerconfigjson
data:
  # This will be generated by the deployment script
  # .dockerconfigjson: <GENERATED_BY_SCRIPT>